apiVersion: v1
data:
  handlingdburl: "{{ .Values.datasources.dburl }}"
  rabbitmqhost: "{{ .Values.datasources.mqhost }}"
  rabbitmqport: "{{ .Values.datasources.mqport }}" 
kind: ConfigMap
metadata:
  name: handling-config